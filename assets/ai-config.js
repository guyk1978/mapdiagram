// Loaded by app/tool.html. AI always uses Supabase Edge `ai-complete` + wallet credits (no browser API keys).
window.MAPDIAGRAM_AI = {
  model: "gpt-4o-mini",
  // Optional: show mock top-up buttons (still hits billing-mock-purchase Edge; requires ALLOW_MOCK_CREDIT_PURCHASES=1).
  useMockCreditPurchases: false,
};
